import SwiftUI

struct SettingsView: View {
    var body: some View {
        ZStack {
            Color.black.ignoresSafeArea()
            Text("Settings Coming Soon")
                .font(.title)
                .foregroundColor(.white)
        }
    }
}

#Preview {
    SettingsView()
}
